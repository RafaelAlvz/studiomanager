/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // You may want to add autoprefixer as well
  },
};

export default config;
